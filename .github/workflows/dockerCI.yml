name: dockerCI

on:
  push:
    branches: [ "master" ]
  # pull_request:
    # branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: ubuntu-latest-run-something
      run: |
        echo "docker pull nginx"
        docker pull nginx
        echo "java -version"
        java -version
        echo "mvn -v"
        mvn -v
        echo "pwd"
        pwd
        echo "ls -al"
        ls -al
        echo "ls -al /home/runner/work/YaJian/YaJian"
        ls -al /home/runner/work/YaJian/YaJian
        echo "mvn clean package"
        # mvn clean package
